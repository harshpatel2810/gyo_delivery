<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.goyo.grocery_goyo.HomeActivity"
    android:weightSum="1">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:orientation="horizontal"
        android:background="@color/colorPrimary"
        >
        <Button
            android:id="@+id/btnResturantSearch"
            android:layout_width="303dp"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary"
            android:drawableLeft="@drawable/ic_action_search_white"
            android:onClick="onSearchPage"
            android:paddingLeft="10dp"
            android:paddingTop="5dp"
            android:layout_weight="1"
            android:singleLine="true"
            android:text="@string/ButtonSearchText"
            android:textAlignment="textStart"
            android:textAllCaps="false"
            android:textColor="#FFFFFF" />
        <ImageView
            android:layout_height="wrap_content"
            android:layout_width="wrap_content"
            android:layout_weight="1"
            android:id="@+id/imageFilter"
            android:layout_gravity="center"
            android:src="@drawable/ic_action_name"
            android:visibility="visible">

        </ImageView>
      </LinearLayout>
    <ListView
        android:id="@+id/list_display_resturants"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/frame_container">
    </FrameLayout>
</LinearLayout>